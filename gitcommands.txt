git basic commands
git status 
git init (init a new respository in local directory)
git add filename (add file to staging area)
git add .   (all file will will add in staging area)

git commit -m "new file added"

git config --global user.email "you@example.com"s
  git config --global user.name "Your Name"

  git log (who did what changes when )
  git diff (know how the files is changes)

  some changes