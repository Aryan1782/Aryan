git basic commands
git status 
git init (init a new respository in local directory)
git add filename (add file to staging area)
git add .   (all file will will add in staging area)

git commit -m "new file added"

git config --global user.email "you@example.com"s
  git config --global user.name "Your Name"

  git log (who did what changes when )
  git diff (know how the files is changes)

  git checkout -b third-branch(creat and switch to third branch)


  git remote and and origin url(connect local repositorym to remote respository)




git ls-files(files avalible in staging area)
git rm --cached filename(you want to keep the file in our hard disk but you dont want git to track a particular file anymore)
git rm filename(remove file from staging area and local direcctory)

BRANCH
git branch (return all branch)
git branch second-branch(creat another branch whose name is second branch)
git checkout second-branch(switch to second branch)


git push -u origin master(push changes to remote respository)





